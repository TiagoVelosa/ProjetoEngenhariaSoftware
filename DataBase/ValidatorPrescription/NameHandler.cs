
using ClassLibraryEngSoft.Authentication.RegisterAuthentication;
using DataBase.Modules;

namespace ClassLibraryEngSoft.ValidatorPrescription
{
    public class NameHandler : BaseHandlerPrescription
    {


        /*public override object Validator(Request data)
        {
            if (data.Data is Prescription prescription)
            {
                
                foreach (var item in prescription.Items)
                {
                   // if(item.Name )
                }
            }

            
        }*/
    }
}